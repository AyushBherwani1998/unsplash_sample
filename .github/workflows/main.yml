name: main action
on:
 push:
    branches: [main]
 pull_request:
    branches: [main]
 workflow_dispatch:
jobs:
  flutter_tests:
    name: Drive tests
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: subosito/flutter-action@v2
      with: 
       channel: master
    - name: Create .env file
      run: |
        cat > lib/core/env/.env <<EOF
        UNSPLASH_API_KEY = ""
        EOF
    - run: flutter test
