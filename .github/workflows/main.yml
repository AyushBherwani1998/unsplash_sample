name: main action
on:
 push:
    branches: [main]
 pull_request:
    branches: [main]
 workflow_dispatch:
jobs:
  flutter_tests:
    name: Drive tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: subosito/flutter-action@v2
      with: 
       channel: master
    - run: flutter test
